# Database (PostgreSQL - Supabase, Neon, etc.)
# Conexión con pooler (para la app). En Supabase: Connection String > Session pooler > URI (puerto 6543, ?pgbouncer=true)
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Conexión directa (para migraciones). En Supabase: mismo usuario/password, host pooler puerto 5432 o "Direct connection" (db.PROJECT_REF.supabase.co:5432)
DIRECT_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"

# Node Environment
# En producción, Vercel lo configura automáticamente como "production"
NODE_ENV="development"

# NextAuth.js
# Genera una clave secreta aleatoria para producción
# Puedes usar: openssl rand -base64 32
NEXTAUTH_SECRET="tu-clave-secreta-super-segura-aqui"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth
# Obtén estas credenciales desde: https://console.cloud.google.com/apis/credentials
# 1. Crea un proyecto en Google Cloud Console
# 2. Habilita la API de Google+
# 3. Crea credenciales OAuth 2.0
# 4. Agrega las URLs autorizadas:
#    - http://localhost:3000 (desarrollo)
#    - https://tu-dominio.com (producción)
# 5. Agrega las URLs de redirección autorizadas:
#    - http://localhost:3000/api/auth/callback/google (desarrollo)
#    - https://tu-dominio.com/api/auth/callback/google (producción)
GOOGLE_CLIENT_ID="tu-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="tu-google-client-secret"

# Opcional: Acceso beta (hash bcrypt de la contraseña; si no se define, se usa la por defecto)
# BETA_ACCESS_HASH=""

# Opcional: Configuración de email (si planeas enviar emails)
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_USER="noreply@diligenz.com"
# SMTP_PASS="password"
